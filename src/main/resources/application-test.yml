spring:
  datasource:
    url: jdbc:mysql://120.26.120.124:3306/iam?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Lmm.887899
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      host: localhost
      port: 6379
      password:

dubbo:
  application:
    name: Dubbo-IAM-Center
  registry:
    address: zookeeper://101.34.8.116:2181
  protocol:
    name: dubbo
    port: -1
  consumer:
    check: false
    lazy: true